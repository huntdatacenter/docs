/* Hide external repository in regular navbar */
.nav-links .repo-link {
  display: none;
}

/* Hide navbar in sidebar. Only works in build, https://github.com/vuejs/vuepress/issues/1099 */
.sidebar .nav-links {
  display: none;
}

.custom-block.details {
  display: block;
  position: relative;
  border-radius: 2px;
  margin: 1.6em 0;
  padding: 1.6em;
  background-color: #eee;
}
.custom-block.details h4 {
  margin-top: 0;
}
.custom-block.details figure:last-child {
  padding-bottom: 0;
}
.custom-block.details p:last-child {
  padding-bottom: 0;
}
.custom-block.details summary {
  font-weight: bold;
  outline: none;
  cursor: pointer;
}
/*
.custom-block.tip {
  border-color: #2e7578;
}
.custom-block.warning {
  border-color: #f4ac67
  background-color: #fdd9b5
}
*/

/* Render code blocks better with line breaks. See https://developer.mozilla.org/en-US/docs/Web/CSS/box-decoration-break */
code {
  -webkit-box-decoration-break: clone;
  box-decoration-break: clone;
}
